# Configuración para producción
# Copia este archivo a .env y completa los valores

# Puerto donde correra la app
PORT=23457

# Environments en formato JSON (escapado para Docker)
# Genera esto con: python -c "import json; print(json.dumps(json.load(open('config.json'))['environments']))"
FORMBRICKS_ENVIRONMENTS='[{"name":"vanity","label":"Vanity","env_type":"prod","group":"Vanity","base_url":"https://feedback.soul23.cloud","api_key":"TU_API_KEY","environment_id":"ID_DEL_ENVIRONMENT"}]'